{
  "name": "aitu",
  "version": "1.0.0",
  "description": "喵闪ai",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "clean": "rm -rf ./packages/*/node_modules node_modules && pnpm i",
    "dev:gly": "pnpm -F \"gallery\" serve",
    "build:gly-test": "pnpm -F \"gallery\" build:test",
    "build:gly-prod": "pnpm -F \"gallery\" build",
    "dev:adm": "pnpm -F \"admin\" serve",
    "build:adm-test": "pnpm -F \"admin\" build:test",
    "build:adm-prod": "pnpm -F \"admin\" build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.4",
    "md5": "^2.3.0"
  },
  "devDependencies": {
    "@types/md5": "^2.3.5"
  }
}